<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.repository.mapper.MemberMapper">

	<select id="login" parameterType="member" resultType="member">
		select *
		from member
		where id = #{id} and pw = #{pw}
	</select>
	
	<select id="selectAllMember" resultType="member">
		select *
		from member
	</select>

	<select id="selectMemberById" parameterType="string" resultType="member">
		select *
		from member
		where id = #{id}
	</select>
	
	<insert id="insertMember" parameterType="member">
		insert into member (id, pw, name, email, address, grade)
		values (#{id}, #{pw}, #{name}, #{email}, #{address}, #{grade})
	</insert>
	
	<update id="updateMemberInfo" parameterType="member">
		update member
		set pw = #{pw}, name = #{name}, email = #{email}, address = #{address}
		where id = #{id}
	</update>

	<update id="updateMemberGrade" parameterType="member">
		update member
		set grade = #{grade}
		where id = #{id}
	</update>
	
	<delete id="deleteMember" parameterType="string">
		delete from member
		where id = #{id}
	</delete>
	
</mapper>